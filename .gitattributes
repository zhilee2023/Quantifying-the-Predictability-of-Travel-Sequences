# Git LFS — GitHub rejects blobs > 100 MB without LFS
# Note: GitHub LFS single-object limit is 2 GB; do not track larger CSVs (e.g. latent_code_occurrences).
*.pt filter=lfs diff=lfs merge=lfs -text
*.pkl filter=lfs diff=lfs merge=lfs -text
